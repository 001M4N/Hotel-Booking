export const hotelImages = [...Array(50)].map(
  (_, index) => `hotel-booking/public/assets/hotelImages/${index}.webp`
);

export const roomImages = [...Array(50)].map(
  (_, index) => `hotel-booking/public/assets/roomImages/${index}.webp`
);
