export const hotelImages = [...Array(50)].map(
  (_, index) => `/assets/hotelImages/${index + 1}.webp`
);

export const roomImages = [...Array(50)].map(
  (_, index) => `/assets/roomImages/${index + 1}.webp`
);
